<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.biu.ufo"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <application
        android:name="org.biu.ufo.MainApplication_"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        
        <service android:name="com.openxc.VehicleManager" />
        
        <service
            android:name="com.openxc.remote.VehicleService"
            android:process=":remote" >
            <intent-filter>
                <action android:name="com.openxc.remote.VehicleServiceInterface" />
            </intent-filter>
        </service>
        
        <service android:name="org.biu.ufo.CarGatewayService_" />

        <receiver android:name="org.biu.ufo.BootupReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name="org.biu.ufo.UfoMainService"
            android:enabled="true"
            android:exported="true" >
        </service>

    </application>

</manifest>