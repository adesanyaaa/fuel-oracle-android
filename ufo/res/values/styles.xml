<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <!-- Style for Windows -->
    <style name="Theme.FloatingDialog" parent="@android:style/Theme.Holo.Light.Dialog.NoActionBar.MinWidth">
        <item name="android:windowBackground">@null</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>

    <!-- Style for Cards -->
    <style name="card.content_outer_layout">
        <item name="android:layout_width">match_parent</item>
        <item name="android:minHeight">@dimen/card_base_empty_height</item>
        <item name="android:layout_marginTop">@dimen/card_content_outer_view_margin_top</item>
        <item name="android:layout_marginBottom">@dimen/card_content_outer_view_margin_bottom</item>
        <item name="android:layout_marginLeft">@dimen/card_content_outer_view_margin_left</item>
        <item name="android:layout_marginRight">@dimen/card_content_outer_view_margin_right</item>
    </style>

    <style name="Birth.card.content_outer_layout" parent="@style/card.content_outer_layout">
        <item name="android:minHeight">60dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_marginTop">@dimen/card_content_outer_view_margin_top</item>
        <item name="android:layout_marginBottom">@dimen/card_content_outer_view_margin_bottom</item>
        <item name="android:layout_marginLeft">@dimen/card_content_outer_view_margin_left</item>
        <item name="android:layout_marginRight">@dimen/card_content_outer_view_margin_right</item>
    </style>

    <style name="card.main_layout_expandinside" parent="card.main_layout">
        <item name="android:background">@drawable/card_background</item>
    </style>

    <style name="card.header_with_items_title">
        <item name="android:textColor">@color/card_text_color_header</item>
        <item name="android:textSize">@dimen/card_header_with_items_title_text_size</item>
        <item name="android:layout_marginTop">@dimen/card_header_simple_title_margin_top</item>
        <item name="android:layout_marginBottom">@dimen/card_header_simple_title_margin_bottom</item>
    </style>

    <style name="card_action_button">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="popup">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:layout_marginLeft">@dimen/popup_margin_left</item>
    </style>

    <style name="popup.header">
        <item name="android:textSize">@dimen/popup_header_text_size</item>
        <item name="android:layout_marginTop">@dimen/popup_margin_top_header</item>
    </style>

    <style name="popup.sub_header">
        <item name="android:textSize">@dimen/popup_sub_header_text_size</item>
    </style>

</resources>